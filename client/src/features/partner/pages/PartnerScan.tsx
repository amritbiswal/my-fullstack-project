import { Card } from "../../../components/ui/Card";

export function PartnerScan() {
  return (
    <Card className="p-4">
      <div className="text-base font-semibold">Partner Scan</div>
      <div className="mt-1 text-sm text-slate-600">QR scan + consent result UI will be added next.</div>
    </Card>
  );
}
